
## AI 提示词生成卡片方案整理（2026-08-31）
- 梳理了 AGenUI 卡片生成链路：skills/a2ui-generation（SKILL.md + reference + validate_a2ui.py）→ studio prompt_builder 拼装 system prompt（含从校验器自动生成的契约段）→ LLM 双 JSON 块 → extractor + validate 校验 → 失败修复重试 → C++ core 渲染。
- 组件精准性四层防线：catalog Schema / prompt allowlist / 校验器契约注入（prompt 与 validator 同源，_render_validator_contract）/ 生成后强校验+模型审查。
- 新增组件路径：core 组件规格 → 三端实现与注册（Android @BuiltInComponent 注解 / iOS ComponentRegister / 鸿蒙 default_registry）→ skill catalog 文档 + validate_a2ui.py 白名单 → agenui_catalog.json → README/Playground/测试。
- 产出文档：docs/卡片生成与组件扩展方案.md
