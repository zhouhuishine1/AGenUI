# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "工具集"

[setup]
script = ""

[[actions]]
name = "合并到main分支"
icon = "tool"
command = "orig_branch=$(git branch --show-current) && ts=$(date '+%Y-%m-%d %H:%M:%S %z') && prompt='自动合并并同步main后回切原分支' && git add -A && if ! git diff --cached --quiet; then git commit -m \"chore: ${prompt} [${ts}]（提交当前分支未提交变更）\"; fi && git push -u origin \"$orig_branch\" && git fetch origin && git checkout main && git pull --ff-only origin main && git checkout \"$orig_branch\" && git merge main && git add -A && if ! git diff --cached --quiet; then git commit -m \"chore: ${prompt} [${ts}]（合并main到当前分支）\"; fi && git push && git checkout main && git merge \"$orig_branch\" && git push origin main && git checkout \"$orig_branch\" && git add -A && if ! git diff --cached --quiet; then git commit -m \"chore: ${prompt} [${ts}]（回切后最终提交）\"; git push; fi"

[[actions]]
name = "合并到main分支"
icon = "tool"
command = "orig_branch=$(git branch --show-current) && ts=$(date '+%Y-%m-%d %H:%M:%S %z') && prompt='自动合并并同步main后回切原分支' && git add -A && if ! git diff --cached --quiet; then git commit -m \"chore: ${prompt} [${ts}]（提交当前分支未提交变更）\"; fi && git push -u origin \"$orig_branch\" && git fetch origin && git checkout main && git pull --ff-only origin main && git checkout \"$orig_branch\" && git merge main && git add -A && if ! git diff --cached --quiet; then git commit -m \"chore: ${prompt} [${ts}]（合并main到当前分支）\"; fi && git push && git checkout main && git merge \"$orig_branch\" && git push origin main && git checkout \"$orig_branch\" && git add -A && if ! git diff --cached --quiet; then git commit -m \"chore: ${prompt} [${ts}]（回切后最终提交）\"; git push; fi"
